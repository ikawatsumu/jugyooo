
cker / PHP）

このリポジトリは、PHPとNginx環境をDocker Composeを用いて構築したWebアプリケーション（掲示板）プロジェクトです。

---

## 🚀 起動方法

コンテナのビルドおよび起動は以下のコマンドで行います。

```bash
# コンテナのビルド（初回・設定変更時）
docker compose build

# コンテナの起動
docker compose up -d
